# KosManager ops — salin ke .env, JANGAN commit .env asli.
# Semua nilai di bawah placeholder; compose menolak start tanpa-password.

MYSQL_PASSWORD=ISI_PASSWORD_DB
MYSQL_ROOT_PASSWORD=ISI_ROOT_PASSWORD_DB
JWT_SECRET=ISI_MIN_32_KARAKTER_ACAK

# Opsional — default ../api (working tree lokal)
# KOS_API_CONTEXT=https://github.com/RakhaYandra/kosmanager-dotnet.git
